/**
 * This low level module is for dealing with Resource Interchange File Format (RIFF). Typically if you are going to
 * encode or decode `.wav` files, you likely don't have to touch this module.
 * 
 * @module
 */

export * from "./riff.ts";
export * as wave from "./wave.ts";